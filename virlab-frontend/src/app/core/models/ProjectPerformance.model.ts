 // Define an interface for your chart data points for better type safety
export interface ProjectPerformance {
  projectName: string;
  performanceScore: number; // This will determine the bar height
}